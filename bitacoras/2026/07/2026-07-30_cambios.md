# Perímetro · jueves 30 de julio de 2026

> Parte de la jornada. Toda cifra está medida contra el repositorio o contra los
> documentos leídos; ninguna está estimada.

## Qué se hizo

**Revisión crítica de los tres documentos existentes.** Manual de construcción,
maqueta HTML del panel y plan por fases.

**Hallazgo principal — las cifras de la maqueta no reconcilian, y la causa está en
el esquema de la fase 0.** Sobre los mismos 190 casos, el panel muestra `41`
escalados a humano en el KPI y `17` en el reparto del enrutador. Ambas cifras son
internamente coherentes: `78.4 %` resueltos sin humano da `41`, y `121 + 52 + 17`
da `190`. Son incompatibles porque el enum `resultado` mezcla el desvío de local a
nube con el escalado a un humano. Corregido en el esquema antes de escribir el
primer evento → R‑002.

**Otras contradicciones medidas en la maqueta:**

| Cifra mostrada | Lo que implica el resto del panel |
|---|---|
| Costo por caso resuelto `$0.037` | `$3.41 ÷ 149 resueltos = $0.023` |
| Gasto del mes `$3.41` | `149 × $0.037 = $5.51`, sobre un periodo de 14 días |
| Vigía de perímetro `0 de 0` | Denominador cero: la afirmación central no está probada |
| `8.9 %` escalado y `8.9 %` bloqueado por sustento | Mismo número en dos métricas distintas |

**Revisión del plan por fases.** Once observaciones, de las que salieron nueve
decisiones de diseño.

**Escrita la propuesta de desarrollo por fases.** Dieciséis fases, con la
redistribución de la 4C, la partición de la 4B, el orden 7 → 6B, y una fase 8 de
despliegue que el plan no tenía.

**Entra Firebase, entra git y GitHub, sale n8n.** Arquitectura en dos planos con
proyección de un solo sentido, y un invariante 8 que la sostiene. n8n queda como
referencia documentada.

**Montado el vault de Obsidian** en `docs/`, con `INDEX`, `00-CANON`,
`CALL_CENTRE_DOCS` y `REFERENCIA-N8N`, más la habilidad `call_centre_docs` que lo
mantiene.

**Leída la referencia de n8n** — cinco capturas del lienzo. Confirma seis piezas
del plan y contradice tres. La más importante: su verificador de respuesta usa un
*Auto Fixing Output Parser*, o sea un modelo corrigiendo a otro modelo, que es lo
que el invariante 7 prohíbe. De ahí salió R‑003.

## Qué quedó abierto

- **El corpus de la empresa ficticia.** No existe. Bloquea la fase 2 y la 7.
- **La máquina de referencia para Ollama.** Sin definir. Sin ella no hay cifra de
  costo local defendible.
- **El proveedor de nube** para inferencia frontera. Sin decidir.
- **El trámite de WhatsApp Business.** Sin iniciar. Puede tardar semanas.
- **Los arreglos de la maqueta.** Diagnosticados y sin aplicar: contraste de
  `--faint` en 2.6:1 sobre blanco cuando AA pide 4.5, estado de vigías comunicado
  solo por color, filas de tabla con `cursor:pointer` pero no operables con teclado,
  gráfico de barras sin escala, y fuentes servidas desde `fonts.googleapis.com` en
  un producto cuya tesis es que los datos no salen del perímetro.
- **El repositorio no es un repositorio de git todavía.** `git init` pendiente.

## Entradas de manual generadas

R‑001 a R‑011. Ver [[CALL_CENTRE_DOCS]] §Revisión 2026‑07‑30.

## Nada medido

No hubo ejecución de código en esta jornada, porque no existe código. Todas las
cifras de arriba salen de leer los documentos, no de correr nada.
